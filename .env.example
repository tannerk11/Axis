# PostgreSQL connection string
# For local development: postgresql://user:password@localhost:5432/axis_analytics
# For Render: provided automatically via DATABASE_URL environment variable
DATABASE_URL=postgresql://user:password@localhost:5432/axis_analytics

# Environment (development or production)
# The scheduler only runs in production
NODE_ENV=development

# Current season (used by data pipeline scripts)
SEASON=2025-26

# Frontend URL for CORS (production only)
# FRONTEND_URL=https://naia-analytics.onrender.com
